<!DOCTYPE html>
<html lang="en-EN">
<head>
  <title>User Profile</title>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container py-4">

  <div class="row">
    <div class="col-12">
      <div class="user-info d-flex justify-content-between" style="gap: 15px">
        <div>
          <% if @user.photo && @user.photo.match(/\.(png|jpg|jpeg|gif)$/i) %>
            <img src="<%= @user.photo %>" alt="User Photo" class="user-photo" style="object-fit:cover; width: 80px">
          <% else %>
            <div class="user-photo" style="width: 150px; height: 150px;">User<%= @user.id %> Pic</div>
          <% end %>
        </div>
        <div class="d-flex justify-content-between border p-3" style="flex-basis: 90%">
          <h1><%= @user.name %></h1>
          <p class="mt-4"><%= link_to "Number of posts: #{@user.posts_counter}", user_posts_path(@user), class: "text-muted" %></p>
        </div>
      </div>
    </div>
  </div>

  <!-- Bio Section -->
  <div class="row">
    <div class="col-12 my-3">
      <div class="p-3 border rounded bio-section">
        <h5><%= @user.name %> Bio</h5>
        <p><%= @user.bio %></p>
      </div>
    </div>
  </div>

  <!-- Posts Section -->
  <div class="row px-3">
    <div class="col-12 my-3 border p-3">
    <h5 class="user-name">
      Most Recent Posts:
    </h5>
    <% recent_posts = @user.recent_posts %>
    <% if recent_posts.any? %>
      <% recent_posts.each do |post| %>
        <div class="col-sm-12 custom-border rounded my-3">
          <h5 class="user-name py-3">
            Post #<%= post.id %>
          </h5>
          <h3 class="user-name">
            <%= post.title %>
          </h3>
          <p class="user-name">
            <%= post.text %>
          </p>
          <div class="text-end">
            <p><%= post.comments.count %> comments</p>
            <p><%= post.likes.count %> Likes</p>
          </div>
        </div>
      <% end %>
    <% else %>
      <p class="custom-border py-2">No recent posts</p>
    <% end %>
    </div>
  </div>

<div>
  <div class="container mt-4">
    <div class="row">
      <div class="col text-center mb-3">
        <%= link_to "See all posts", user_posts_path(@user), class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
</div>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
