<!DOCTYPE html>
<html lang="en-EN">
<head>
  <title>All Users</title>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <h1 class="mt-5">All Users</h1>
  <p class="lead">Here you can see all the users of the blog.</p>
  <div class="row">
    <ul class="list-group mt-2 mb-4 w-100">
      <% @users.each do |user| %>
        <li class="list-group-item my-2 border-0">
          <div class="media align-items-center">
            <div class="user-photo mr-3 border p-3">
              <% if user.photo && user.photo.match(/\.(png|jpg|jpeg|gif)$/i) %>
                <img src="<%= user.photo %>" alt="User Photo" style="width: 150px; height: 150px;">
              <% else %>
                <div class="placeholder-image" style="width: 150px; height: 150px;">
                  PhotoUser<%= user.id %>
                </div>
              <% end %>
            </div>
            <div class="media-body d-flex border px-3 py-5">
              <h3 class="font-weight-bolder w-50">
                <%= link_to user.name, user_path(user), title: "To User Details", class: "d-block stretched-link" %>
              </h3>
              <p class="user-posts text-right w-50 mt-5">
                Number of posts: <%= user.posts_counter %>
              </p>
            </div>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</div>

<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
